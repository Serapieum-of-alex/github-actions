name: 'Setup Python with uv'
description: 'Setup Python environment with uv package manager and install dependencies'

inputs:
  python-version:
    description: 'Python version to install'
    required: false
    default: '3.12'
  install-groups:
    description: 'Dependency groups (from [dependency-groups]) and/or optional dependencies (from [project.optional-dependencies]) to install. Format: "dev", "dev docs", "extra:aws extra:viz". Prefix optional dependencies with "extra:". Leave empty to install only core dependencies.'
    required: false
    default: ''
  verify-lock:
    description: 'Whether to verify the lock file is up to date'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: ${{ inputs.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v4
      with:
        enable-cache: true
        cache-dependency-glob: uv.lock

    - name: Verify lock file is up to date
      if: inputs.verify-lock == 'true'
      shell: bash
      run: |
        echo "::group::Verifying uv.lock file is up to date"
        uv lock --check
        echo "::endgroup::"

    - name: Install Dependencies
      shell: bash
      run: |
        echo "::group::Preparing uv sync command"

        # Start with frozen flag
        args=("--frozen")

        # Initialize flags for tracking what we're installing
        has_groups=false
        has_extras=false

        # Check if install-groups is provided
        if [[ -n "${{ inputs.install-groups }}" ]]; then
          echo "Processing install-groups: ${{ inputs.install-groups }}"

          # Split groups on space and comma
          IFS=' ,' read -r -a items <<< "${{ inputs.install-groups }}"

          # Process each item
          for item in "${items[@]}"; do
            # Remove leading/trailing whitespace
            item=$(echo "$item" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
            if [[ -n "$item" ]]; then
              # Check if this is an optional dependency (prefixed with "extra:")
              if [[ "$item" == extra:* ]]; then
                # Extract the extra name after "extra:"
                extra_name="${item#extra:}"
                args+=("--extra" "$extra_name")
                has_extras=true
                echo "  - Adding optional dependency (extra): $extra_name"
              else
                # Regular dependency group
                args+=("--group" "$item")
                has_groups=true
                echo "  - Adding dependency group: $item"
              fi
            fi
          done

          # Only add --no-default-groups if we have dependency groups
          if [[ "$has_groups" == true ]]; then
            # Insert --no-default-groups after --frozen but before any --group flags
            args=("--frozen" "--no-default-groups" "${args[@]:1}")
          fi
        else
          echo "Installing core dependencies only (no optional groups or extras)"
          # Install without any optional groups
          args+=("--no-default-groups")
        fi

        echo "Running: uv sync ${args[*]}"
        echo "::endgroup::"

        echo "::group::Installing dependencies with uv"
        uv sync "${args[@]}"
        echo "::endgroup::"

    - name: Activate virtual environment
      shell: bash
      run: |
        echo "::group::Activating virtual environment"
        # Determine the correct bin/Scripts directory
        if [ -f .venv/bin/python ]; then
          VENV_BIN="$(pwd)/.venv/bin"
          echo "Adding to PATH: $VENV_BIN"
          echo "$VENV_BIN" >> $GITHUB_PATH
          echo "VIRTUAL_ENV=$(pwd)/.venv" >> $GITHUB_ENV
        elif [ -f .venv/Scripts/python.exe ]; then
          VENV_SCRIPTS="$(pwd)/.venv/Scripts"
          echo "Adding to PATH: $VENV_SCRIPTS"
          echo "$VENV_SCRIPTS" >> $GITHUB_PATH
          echo "VIRTUAL_ENV=$(pwd)/.venv" >> $GITHUB_ENV
        else
          echo "::warning::No virtual environment found at .venv"
        fi
        echo "::endgroup::"

    - name: Show environment information
      shell: bash
      run: |
        echo "::group::Environment information"
        echo "Virtual environment: ACTIVATED"
        echo ""
        echo "Virtual environment location:"
        if [ -d .venv ]; then
          echo "  $(pwd)/.venv"
          echo ""
          echo "Python executable:"
          if [ -f .venv/bin/python ]; then
            echo "  $(pwd)/.venv/bin/python"
          elif [ -f .venv/Scripts/python.exe ]; then
            echo "  $(pwd)/.venv/Scripts/python.exe"
          fi
          echo ""
          echo "The virtual environment has been automatically activated."
          echo "You can now use 'python' and installed CLI tools directly in subsequent steps."
          echo ""
          echo "Example usage:"
          echo "  - run: python --version"
          echo "  - run: pytest"
          echo "  - run: black ."
        else
          echo "  No .venv directory found"
        fi
        echo "::endgroup::"

        echo "::group::Installed packages summary"
        python --version
        uv pip list
        echo "::endgroup::"