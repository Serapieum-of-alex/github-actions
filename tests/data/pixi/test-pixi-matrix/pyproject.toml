[project]
          name = "invalid-test"
          version = "0.1.0"
          dependencies = []

          [tool.pixi.workspace]
          channels = ["conda-forge"]
          
          [tool.pixi.dependencies]
          python = ">=99.0"  # Impossible version